# Normalise line endings to LF in the repo on every platform.
* text=auto eol=lf

# Scripts that run inside the Linux container must stay LF.
*.sh text eol=lf

# Binary assets — never touch.
*.ico binary
*.png binary
*.jpg binary
*.webp binary
*.woff binary
*.woff2 binary
